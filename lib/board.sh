# bench board — status-driven crew relayout (nav wave T-B).
# Contract: docs/nav_wave_spec.md §2. Deterministic, transition-gated, layout
# only; never touches task state, never answers prompts.

cmd_board() {
  die "board: not implemented yet — nav-wave task T-B builds this (docs/nav_wave_spec.md §2)"
}
